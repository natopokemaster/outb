<?xml version="1.0" encoding="UTF-8"?>
<ehcache>

    <!-- Sets the path to the directory where cache .data files are created.

         If the path is a Java System Property it is replaced by
         its value in the running VM.

         The following properties are translated:
         user.home - User's home directory
         user.dir - User's current working directory
         java.io.tmpdir - Default temp file path -->
    <diskStore path="java.io.tmpdir"/>

    <!-- Default Cache configuration. These will applied to caches programmatically created through
        the CacheManager.

        The following attributes are required:

        maxElementsInMemory            - Sets the maximum number of objects that will be created in memory
        eternal                        - Sets whether elements are eternal. If eternal,  timeouts are ignored and the
                                         element is never expired.
        overflowToDisk                 - Sets whether elements can overflow to disk when the in-memory cache
                                         has reached the maxInMemory limit.

        The following attributes are optional:
        timeToIdleSeconds              - Sets the time to idle for an element before it expires.
                                         i.e. The maximum amount of time between accesses before an element expires
                                         Is only used if the element is not eternal.
                                         Optional attribute. A value of 0 means that an Element can idle for infinity.
                                         The default value is 0.
        timeToLiveSeconds              - Sets the time to live for an element before it expires.
                                         i.e. The maximum time between creation time and when an element expires.
                                         Is only used if the element is not eternal.
                                         Optional attribute. A value of 0 means that and Element can live for infinity.
                                         The default value is 0.
        diskPersistent                 - Whether the disk store persists between restarts of the Virtual Machine.
                                         The default value is false.
        diskExpiryThreadIntervalSeconds- The number of seconds between runs of the disk expiry thread. The default value
                                         is 120 seconds.
        -->

    <defaultCache
        maxElementsInMemory="15000"
        eternal="false"
        timeToIdleSeconds="14400"
        timeToLiveSeconds="28800"
        overflowToDisk="@cache.overflow.to.disk@"
        diskPersistent="false"
        diskExpiryThreadIntervalSeconds="120"
        />

    <!-- Predefined caches.  Add your cache configuration settings here.
        If you do not have a configuration for your cache a WARNING will be issued when the
        CacheManager starts

        The following attributes are required:

        name                           - Sets the name of the cache. This is used to identify the cache. 
                                         It must be unique.
        maxElementsInMemory            - Sets the maximum number of objects that will be created in memory
        eternal                        - Sets whether elements are eternal. If eternal,  timeouts are ignored and the
                                         element is never expired.
        overflowToDisk                 - Sets whether elements can overflow to disk when the in-memory cache
                                         has reached the maxInMemory limit.

        The following attributes are optional:
        timeToIdleSeconds              - Sets the time to idle for an element before it expires.
                                         i.e. The maximum amount of time between accesses before an element expires
                                         Is only used if the element is not eternal.
                                         Optional attribute. A value of 0 means that an Element can idle for infinity.
                                         The default value is 0.
        timeToLiveSeconds              - Sets the time to live for an element before it expires.
                                         i.e. The maximum time between creation time and when an element expires.
                                         Is only used if the element is not eternal.
                                         Optional attribute. A value of 0 means that and Element can live for infinity.
                                         The default value is 0.
        diskPersistent                 - Whether the disk store persists between restarts of the Virtual Machine.
                                         The default value is false.
        diskExpiryThreadIntervalSeconds- The number of seconds between runs of the disk expiry thread. The default value
                                         is 120 seconds.
        -->
    <!-- Place configuration for your caches following -->
	<cache name="net.sf.hibernate.cache.UpdateTimestampsCache"
        maxElementsInMemory="86400000"
        eternal="true"
        overflowToDisk="@cache.overflow.to.disk@"/>    
    
    <!-- Configuracoes de cache do proxy -->
    <cache name="ServiceFactorySERVICE"
        maxElementsInMemory="2000"
        eternal="false"
        timeToLiveSeconds="86400000"
        overflowToDisk="false"/>
        
    <cache name="ServiceFactoryTASK"
        maxElementsInMemory="2000"
        eternal="false"
        timeToLiveSeconds="86400000"
        overflowToDisk="false"/>
        
    <cache name="ServiceFactoryWEB"
        maxElementsInMemory="5000"
        eternal="false"
        timeToLiveSeconds="86400000"
        overflowToDisk="false"/>
    
	<!-- ================================================================= -->
	<!-- 	   	NETSMS - MODULO COBRANCA EH Cache Query Provider Configurations		   -->
	<!-- ================================================================= -->
    <cache name="NETCRMCORE_BASE_ISP_CACHE.query.cache.region"
        maxElementsInMemory="5000"
        eternal="false"
        timeToLiveSeconds="7200"
        overflowToDisk="@cache.overflow.to.disk@"/>
        
    <cache name="NETCRMCORE_BASE_SP_CACHE.query.cache.region"
        maxElementsInMemory="5000"
        eternal="false"
        timeToLiveSeconds="7200"
        overflowToDisk="@cache.overflow.to.disk@"/> 
        	
    <cache name="NETCRMCORE_BASE_BR_CACHE.query.cache.region"
        maxElementsInMemory="5000"
        eternal="false"
        timeToLiveSeconds="7200"
        overflowToDisk="@cache.overflow.to.disk@"/>
        
    <cache name="NETCRMCORE_BASE_SUL_CACHE.query.cache.region"
        maxElementsInMemory="5000"
        eternal="false"
        timeToLiveSeconds="7200"
        overflowToDisk="@cache.overflow.to.disk@"/>  
        
    <cache name="NETCRMCORE_BASE_SOC_CACHE.query.cache.region"
        maxElementsInMemory="5000"
        eternal="false"
        timeToLiveSeconds="7200"
        overflowToDisk="@cache.overflow.to.disk@"/>
        
    <cache name="NETCRMCORE_BASE_BH_CACHE.query.cache.region"
        maxElementsInMemory="5000"
        eternal="false"
        timeToLiveSeconds="7200"
        overflowToDisk="@cache.overflow.to.disk@"/>
        
    <cache name="NETCRMCORE_BASE_VIV_CACHE.query.cache.region"
        maxElementsInMemory="5000"
        eternal="false"
        timeToLiveSeconds="7200"
        overflowToDisk="@cache.overflow.to.disk@"/>        

    <cache name="NETCRMCORE_BASE_CTV_CACHE.query.cache.region"
        maxElementsInMemory="5000" eternal="false"
        timeToLiveSeconds="7200"
        overflowToDisk="@cache.overflow.to.disk@"/>   

</ehcache>